# ROMA

## Description
Learning about ROMA.  
This project breaks complex tasks into subtasks, executes each subtask, and then aggregates the results.  
The code uses the `solve()` function, which works recursively.

## Installation
```bash
git clone https://github.com/sentient-agi/ROMA.git
cd ROMA

